# RF4 Recipe Viewer - Implementation Details

## Directory Structure
```
/
├── index.html           # Main PWA loader and HTML skeleton
├── ui.html              # Application screen template (injected by app.js)
├── sw.js                # Service Worker for offline asset caching
├── manifest.json        # PWA Web Manifest (icons, name, display mode)
├── icon-192.png         # PWA icon
├── icon-512.png         # PWA icon
├── package.json         # Test harness metadata (Node ESM)
├── test_custom_item.js  # Unit tests for the CustomItem model
├── test_smoke.mjs       # DOM-stub end-to-end smoke test (routing, filters, loadout)
│
├── css/
│   └── style.css        # Application styling and layout (Flexbox/Grid)
│
├── js/
│   ├── app.js           # Entrypoint: UI injection, sidebar navigation, a11y
│   ├── components/
│   │   ├── recipe-viewer.js   # Core app logic: routing, decoding, filtering, detail view
│   │   └── loadout-builder.js # Loadout Editor: slots, totals, share/import modal
│   └── models/
│       └── custom-item.js     # Model for custom crafting inheritance trees + stat helpers
│
└── data/
    └── data.json        # Ultra-compressed database generated by the build pipeline
```

Note: `scripts/convert.py` (documented in `specs.md`) is not part of this repository; the
compressed `data/data.json` is checked in directly.

## Data Pipeline
The `data.json` format is produced by an external conversion script (see `specs.md`):
1. **Compression:** every unique string is registered into a global `string_table` (`0` is the
   empty string). Records are arrays of integer IDs with trailing zeros stripped.
2. **Output:** `{"strings": [...], "data": { "<sheetId>": [[...], ...] }}` where `sheetId`
   indexes into `strings` for the sheet name.

## Frontend Logic (`js/components/recipe-viewer.js` & `loadout-builder.js`)
1. **Fetching & Decoding:** Fetches `data/data.json`, resolves string IDs, and reconstructs
   readable recipe objects with properties: `id`, `skill`, `type`, `subtype`, `name`, `level`,
   `materials`, `baseStats`, and `upgradeStats`. Non-craftable items are added from the
   `Item Values` sheet (plus any leftover materials/equipment) as `Ingredient`/`Equipment` items.
2. **Skill Routing:** Differentiates 'Cooking' and 'Chemistry' skills (shared raw sheet) by
   inspecting `type`/`subtype`.
3. **Dynamic Filtering:** Skill and subtype filters update the material grid options; results
   must contain *all* selected materials (count-aware).
4. **Hash Routing:** `#/search`, `#/recipe/<id>`, and `#/loadout` are the single source of truth
   for screen visibility. All screen switches go through `navigateTo`; the browser back/forward
   stack is used for navigation. Pick-mode navigation uses history replacement so it adds no
   back entries. The detail view carries the live `CustomItem` instance so inheritance edits
   persist and mutate the original object.
5. **Inheritance pick mode:** clicking an empty material slot shows a banner on the search
   screen; picking a recipe fills the slot; Cancel (or navigating away) clears pick mode
   without touching filters.
6. **Loadout Editor:** 6 slots (Weapon, Shield, Headgear, Armor, Shoes, Accessory). Weapon
   subtypes map to the Weapon slot. Totals = equipped item `baseStats` + `upgradeStats` of
   every slot material (plain materials and inherited crafted items both contribute their
   upgrade values). Export/import uses a Base64(URI-encoded JSON) code via an in-app modal.
7. **Service Worker:** network-first with cache fallback; cache version `rf4-recipes-v14`.
